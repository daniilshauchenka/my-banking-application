apiVersion: v1
kind: Service
metadata:
  name: cash-service
  namespace: my-bank
spec:
  selector:
    app: cash-service
  ports:
    - name: http
      port: 6083
      targetPort: 6083
    - name: management
      port: 7083
      targetPort: 7083
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cash-service
  namespace: my-bank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cash-service
  template:
    metadata:
      labels:
        app: cash-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cash-service
          image: my-bank/cash-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 6083
            - name: management
              containerPort: 7083
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: my-bank-app-config
            - secretRef:
                name: my-bank-app-secrets
          env:
            - name: SERVER_PORT
              value: "6083"
            - name: MANAGEMENT_PORT
              value: "7083"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: management
            initialDelaySeconds: 40
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: management
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
