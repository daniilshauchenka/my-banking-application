apiVersion: v1
kind: Service
metadata:
  name: cash-postgres
  namespace: my-bank
spec:
  selector:
    app: cash-postgres
  ports:
    - name: postgres
      port: 6434
      targetPort: 6434
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cash-postgres
  namespace: my-bank
spec:
  serviceName: cash-postgres
  replicas: 1
  selector:
    matchLabels:
      app: cash-postgres
  template:
    metadata:
      labels:
        app: cash-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          args: ["postgres", "-p", "6434"]
          ports:
            - containerPort: 6434
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: my-bank-app-config
                  key: CASH_DB_NAME
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: my-bank-app-secrets
                  key: CASH_DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: my-bank-app-secrets
                  key: CASH_DB_PASSWORD
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -p 6434 -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\""]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -p 6434 -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\""]
            initialDelaySeconds: 30
            periodSeconds: 20
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
